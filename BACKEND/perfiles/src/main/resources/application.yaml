server:
  port: 8082

spring:
  application:
    name: perfiles
  datasource:
    url: jdbc:postgresql://localhost:5433/b2bmatch
    username: ${DB_USERNAME:postgres}
    password: ${DB_PASSWORD:postgres}
  jpa:
    properties:
      hibernate:
        default_schema: b2bmatch
    hibernate:
      ddl-auto: validate
    show-sql: true